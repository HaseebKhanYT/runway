# Database (docker compose up -d)
DATABASE_URL=postgresql://runway:runway@localhost:5433/runway

# Clerk — create an app at https://dashboard.clerk.com; keyless dev mode works
# without these for local development of apps/web.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Web -> API base URL
NEXT_PUBLIC_API_URL=http://localhost:8787

# --- Plaid (reserved — not used in v1) ---
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
