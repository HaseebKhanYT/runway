services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: runway
      POSTGRES_PASSWORD: runway
      POSTGRES_DB: runway
    volumes:
      - runway-pgdata:/var/lib/postgresql/data

volumes:
  runway-pgdata:
